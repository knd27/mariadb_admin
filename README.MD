# BMariaDb + phpmyadmin

Docker BMariaDb + phpmyadmin.

## 🚀 Instalasi

### 1. Clone repo
```bash
git clone https://github.com/knd27/mariadb_admin
cd mariadb_admin
mv docker-compose.yml.example docker-compose.yml
```
